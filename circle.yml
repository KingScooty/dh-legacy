machine:
  node:
    version: 5.9.0
  timezone:
    Europe/London
  services:
    - docker

dependencies:
    pre:
    # Install circleci-matrix
    - curl -fsSL https://git.io/v2Ifn | bash

test:
  override:
    # - cd $TEST_DIR && NODE_ENV=test npm test
    - circleci-matrix
