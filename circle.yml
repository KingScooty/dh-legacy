machine:
  node:
    version: 6.2.0
  timezone:
    Europe/London
  services:
    - docker

dependencies:
  pre:
    # Install circleci-matrix
    - curl -fsSL https://git.io/v2Ifn | bash

test:
  override:
    - circleci-matrix
