var chai = require('chai');
var expect = chai.expect;
var cradle = require('cradle');

var db = require('../db');

chai.should();

describe('Database helpers', () => {

  describe('saveDoc()', () => {
    it('', () => {
      console.log(db);
    });
  });

  describe('allDocs()', () => {
    it('', () => {});
  });

  describe('getView()', () => {
    it('', () => {});
  });

});
